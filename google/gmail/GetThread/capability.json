{
  "type": "remote",
  "name": "Gmail Get Thread",
  "description": "Retrieves a full thread with all messages from Gmail.",
  "categories": [
    "Gmail"
  ],
  "tags": [
    "Gmail", "Email", "Thread"
  ],
  "entry_point": "main",
  "auth": {
    "type": "oauth2",
    "env_variable": "GOOGLE_AUTH_TOKEN"
  },
  "organization": "google",
  "group": "gmail",
  "inputs": [
    {
      "name": "threadId",
      "type": "string",
      "required": true,
      "description": "The ID of the thread to retrieve"
    },
    {
      "name": "format",
      "type": "string",
      "required": false,
      "default": "full",
      "description": "The format to return the messages in (full, minimal, metadata)"
    },
    {
      "name": "metadataHeaders",
      "type": "string",
      "required": false,
      "description": "When format=metadata, only include headers specified (comma-separated)"
    }
  ],
  "output": {
    "thread": {
      "type": "object"
    }
  }
}
