{
  "name": "credentials",
  "type": "object",
  "required": true,
  "description": "The credentials object containing the sender's OAuth credentials.",
  "properties": {
    "auth": {
      "type": "object",
      "required": true,
      "description": "The OAuth authentication configuration.",
      "properties": {
        "type": {
          "type": "string",
          "description": "The authentication type.",
          "enum": ["oauth2"]
        },
        "config": {
          "type": "object",
          "required": true,
          "description": "The OAuth configuration.",
          "properties": {
            "scopes": {
              "type": "array",
              "description": "The scopes for the OAuth token.",
              "items": {
                "type": "string"
              }
            },
            "client_id": {
              "type": "string",
              "description": "The client ID for the OAuth application."
            },
            "client_secret": {
              "type": "string",
              "description": "The client secret for the OAuth application."
            },
            "auth_uri": {
              "type": "string",
              "description": "The authorization URI for the OAuth application."
            },
            "token_uri": {
              "type": "string",
              "description": "The token URI for the OAuth application."
            },
            "redirect_uri": {
              "type": "string",
              "description": "The redirect URI for the OAuth application."
            }
          }
        }
      }
    }
  }
}