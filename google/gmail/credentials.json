{
  "name": "credentials",
  "type": "object",
  "required": true,
  "description": "The credentials object containing the sender's email and password.",
  "properties": {
    "senderEmail": {
      "type": "string",
      "description": "The sender's email address."
    },
    "senderPassword": {
      "type": "string",
      "description": "The sender's email password."
    }
  }
}